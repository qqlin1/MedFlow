# MedFlow 门诊资源调度与可靠预约系统

## 校招主项目需求与验收基线 V2.0

| 项目项 | 当前定位 |
|---|---|
| 项目名称 | MedFlow 门诊资源调度与可靠预约系统 |
| 一句话说明 | 围绕有限门诊号源，解决排班冲突、并发预约、重复请求、状态竞争、超时关闭和数据越权问题 |
| 项目性质 | Java 校招主项目、模拟医疗业务系统 |
| 业务范围 | 单家模拟门诊的排班、号源与预约 |
| 核心角色 | 患者 `PATIENT`、医生 `DOCTOR`、管理员 `ADMIN` |
| 架构策略 | 模块化单体；先用 MySQL 保证正确，再引入一个 Redis 场景和一个 RabbitMQ 场景 |
| 核心技术主线 | Spring Security、MySQL 事务与锁、幂等、状态机、Redis Cache Aside、RabbitMQ + Outbox、测试与部署 |
| 计划周期 | 8 周完成可投递版本，之后最多选择一个扩展 |
| 数据边界 | 只使用虚构或脱敏数据，不存储真实患者隐私和真实诊疗信息 |

> 本文档是当前唯一需求基线。V1.0 中排队、叫号、接诊、随访、AI、FHIR 等大范围设计已退出 8 周主线。

---

## 0. 本次重新定位的结论

### 0.1 项目到底是什么

MedFlow 不是“大而全的医院管理系统”，而是：

> 一个以有限号源为核心资源，以预约生命周期为核心状态机，以数据库一致性和可靠异步为技术主线的门诊资源调度系统。

项目要回答的不是“做了多少张表和多少个页面”，而是以下问题：

1. 多个患者同时竞争最后一个号源时，为什么不会超卖？
2. 用户重复点击或网络重试时，为什么只生成一条预约？
3. 预约取消与超时同时发生时，为什么号源只归还一次？
4. 数据库写入成功而消息发送失败时，系统如何恢复？
5. 患者和医生修改请求中的 ID 时，为什么不能越权访问他人数据？
6. 如何用自动化测试、并发测试和执行计划证明方案有效？

### 0.2 它在简历中的角色

| 项目 | 负责证明的能力 |
|---|---|
| OA 系统 | Java Web 基础、分层、JWT、组织树、基础权限和简单状态流转 |
| MedFlow | 跨表事务、资源竞争、幂等、并发状态机、缓存、可靠消息、压测和故障验证 |

因此 MedFlow 不再重复大量用户 CRUD、部门树和普通后台功能。认证仍会实现，但重点是使用 Spring Security 建立标准认证链路和数据权限边界。

### 0.3 完成后的三分钟项目主线

```text
管理员发布有限号源
  -> 患者发起幂等预约
  -> MySQL 在事务中原子扣减号源并创建预约
  -> 患者确认，或系统通过可靠消息超时关闭
  -> 取消与超时竞争时只有一个状态迁移成功
  -> Redis 只加速号源查询，数据库始终是事实源
  -> 并发测试、故障测试和 EXPLAIN 证明方案有效
```

---

## 1. 项目目标与范围边界

### 1.1 必须练到的七类校招能力

1. Java：对象职责、集合、枚举、异常、时间 API、线程池和并发问题。
2. Spring：IOC、MVC 请求链路、Validation、全局异常、AOP 代理和事务。
3. 安全：Spring Security、JWT、RBAC、401/403 和数据归属权限。
4. MySQL：建模、唯一约束、联合索引、事务、MVCC、行锁、死锁和 `EXPLAIN`。
5. Redis：Cache Aside、TTL、缓存穿透、写后失效和故障降级。
6. RabbitMQ：Confirm、Ack、重试、死信、重复消费、Outbox 和最终一致性。
7. 工程：Flyway、自动化测试、Testcontainers、并发验证、Docker Compose、日志和 CI。

### 1.2 P0：MySQL 正确性版本，必须完成

- 项目骨架、统一响应、异常、校验、数据库迁移和接口文档。
- 登录认证、账号状态、三角色权限和数据权限。
- 就诊人、科室、医生、诊室的最小必要数据。
- 排班创建、冲突检查、发布、关闭和 Slot 生成。
- 号源查询。
- 预约创建、确认、取消、查询和状态历史。
- 超时关闭的数据库业务操作先由测试或定时扫描触发，P1 再替换为 RabbitMQ 可靠触发。
- MySQL 条件更新防超卖。
- 请求幂等和业务唯一约束。
- 取消与超时竞争下只归还一次号源。
- 核心规则从开发当周开始配套自动化测试。

### 1.3 P1：校招强化版本，必须完成

- 容量 50、500 个并发请求的预约一致性验证。
- 10 万条预约数据的索引与 `EXPLAIN` 对比。
- Redis 只实现“热点号源查询缓存”，不在主线中扣减库存。
- RabbitMQ 只实现“待确认预约超时关闭”。
- Outbox 投递、重复消费幂等、重试和死信。
- Testcontainers 集成测试。
- Docker Compose、Actuator、Trace ID、结构化业务日志和 CI。
- README、ER 图、状态图、时序图、测试报告和固定演示脚本。

### 1.4 P2：主项目完成后最多选一个

- 签到与简单排队。
- 一次性随访任务。
- Redis Lua 库存预扣、补偿、对账与降级。
- SSE 展示排队或预约状态。
- 敏感数据访问审计 AOP。

P2 只能选择一个。选择条件是 P0、P1 已全部验收并且不影响算法、八股和投递。

### 1.5 当前明确不做

- 护士角色、复杂叫号、过号、重叫和完整接诊流程。
- 处方、药品、缴费、医保、电子病历、LIS、PACS。
- 周期性随访、量表、完整通知矩阵和运营大屏。
- 多医院、多租户和复杂业务字典。
- AI/RAG、FHIR、MinIO、Qdrant、Spring Modulith。
- 微服务、注册中心、网关、分库分表、Kubernetes。
- 完整 Vue 前端；后端阶段使用 OpenAPI 和演示脚本。
- 为了写进简历而同时堆 Redis、Kafka、RabbitMQ 和多个框架。

---

## 2. 核心角色与权限

### 2.1 患者 PATIENT

- 登录和退出。
- 创建、查看和维护自己名下的虚构就诊人。
- 查询未来可预约号源。
- 为自己名下的就诊人创建、确认和取消预约。
- 查看自己名下就诊人的预约和状态历史。
- 不能通过替换 `patientId` 或 `appointmentId` 访问他人数据。

### 2.2 医生 DOCTOR

- 查看自己的排班。
- 查看自己排班下的预约列表和最小必要就诊人信息。
- 不能查看其他医生排班下的预约。
- V1 不实现诊断、处方和完整接诊记录。

### 2.3 管理员 ADMIN

- 维护最小科室、医生和诊室数据。
- 创建、发布、关闭或取消排班。
- 查看号源使用情况和预约列表。
- 管理账号状态。
- 管理员不等于可以无理由读取所有敏感信息；本项目仅提供业务所需字段。

### 2.4 权限的两个维度

```text
角色权限：这个角色能不能调用某类接口
数据权限：这个用户能不能操作这一条具体数据
```

Controller 或安全表达式负责接口入口权限，Service 根据当前用户、患者所有者、医生归属和预约关系完成对象级校验。前端隐藏按钮不能代替后端校验。

---

## 3. 核心领域模型与模块边界

### 3.1 领域对象

| 对象 | 职责 |
|---|---|
| `User` | 登录身份、密码、角色、账号状态 |
| `Patient` | 被预约的就诊人，与登录账号分离 |
| `Department` | 最小科室信息；本项目不重复 OA 的复杂部门树 |
| `Doctor` | 医生业务身份，绑定一个登录账号和一个科室 |
| `ClinicRoom` | 出诊地点 |
| `Schedule` | 某医生在某日期和时间范围内的出诊安排 |
| `Slot` | 从已发布排班生成的可预约时间段及有限容量 |
| `Appointment` | 就诊人对某个 Slot 的预约及当前状态 |
| `AppointmentStatusLog` | 预约状态变化历史 |
| `IdempotencyRecord` | 保存一次幂等请求的处理状态和最终业务结果 |
| `OutboxEvent` | 与业务事务同时落库、等待投递的事件 |
| `ConsumedMessage` | 记录消费者已处理的消息，防止重复副作用 |

### 3.2 模块化单体

```text
com.qqlin.medflow
  identity       认证、账号和当前用户
  patient        就诊人与所有权
  organization   科室、医生、诊室
  scheduling     排班、冲突检查和号源
  appointment    预约、状态机、幂等和容量变化
  reliability    Outbox、MQ 投递和消费记录
  shared         响应、异常、时间、日志和通用配置
```

约束：

- Controller 只处理 HTTP、参数校验和结果转换。
- Service 放业务规则、数据权限、状态机和事务边界。
- Mapper 只负责持久化，不决定业务状态。
- 模块之间不能直接调用对方 Mapper。
- 不因“听起来高级”拆微服务；当前规模下模块化单体更容易保证事务、测试和交付。

---

## 4. 五条核心业务不变量

面试讲解和测试始终围绕以下五条不变量：

1. **不超卖**：`0 <= remainingCapacity <= totalCapacity`。
2. **不重复创建**：相同业务意图不能生成多条有效预约。
3. **不重复归还**：取消或超时只能释放一次号源。
4. **不非法跳转**：预约只能按状态机迁移。
5. **不越权访问**：用户只能访问角色和数据归属都允许的记录。

补充规则：

- 医生和诊室不能存在重叠的有效排班。
- 预约记录与号源扣减必须在同一事务中成功或失败。
- MySQL 是预约和号源的最终事实来源。
- Redis 命中只代表查询结果，不代表预约一定成功。
- RabbitMQ 消息可能重复，消费者必须按重复到达设计。
- 所有截止时间由服务端判断，统一时区。

---

## 5. 核心业务流程

### 5.1 创建并发布排班

```text
管理员提交医生、诊室、日期、开始时间、结束时间
  -> 校验基础数据和时间范围
  -> 按固定顺序锁定医生与诊室资源行
  -> 查询是否存在时间重叠
  -> 创建 DRAFT 排班
  -> 条件更新为 PUBLISHED
  -> 在同一事务中生成固定粒度 Slot
```

重叠判断使用：

```text
existing.startTime < new.endTime
AND existing.endTime > new.startTime
```

“先查冲突再插入”本身存在并发窗口。所有创建排班的事务必须先锁定相同的医生和诊室资源行，并保持固定锁顺序，再检查和插入。

### 5.2 创建预约

请求最小输入：

- Header：`Authorization`、`Idempotency-Key`。
- Body：`patientId`、`slotId`。

事务主流程：

```text
识别当前用户
  -> 获取或占有幂等记录
  -> 校验就诊人属于当前用户
  -> 校验 Slot、排班状态和截止时间
  -> 检查有效重复预约
  -> 条件更新 remaining_capacity = remaining_capacity - 1
     WHERE remaining_capacity > 0 且 Slot 可预约
  -> 插入 PENDING_CONFIRMATION 预约
  -> 插入状态历史
  -> 插入预约超时 Outbox 事件
  -> 保存幂等结果
  -> 提交事务
```

如果扣减成功但预约插入、状态历史或 Outbox 写入失败，整个事务回滚，号源扣减也必须恢复。

### 5.3 确认预约

```text
PENDING_CONFIRMATION
  -> 当前用户拥有该预约
  -> 未超过确认截止时间
  -> CAS 条件更新
  -> BOOKED
  -> 写入状态历史
```

### 5.4 主动取消

```text
PENDING_CONFIRMATION 或 BOOKED
  -> 校验所有权和取消截止时间
  -> CAS 更新为 CANCELLED
  -> 只有 CAS 成功者才能归还一次号源
  -> 写入状态历史
```

重复取消返回已有终态或明确冲突，不能再次增加容量。

### 5.5 超时关闭

```text
预约事务写入 Outbox
  -> Relay 投递延迟消息
  -> RabbitMQ 到期投递
  -> 消费者记录 messageId
  -> CAS: PENDING_CONFIRMATION -> EXPIRED
  -> 只有 CAS 成功才归还号源并写状态历史
  -> 事务提交后 Ack
```

如果患者确认、主动取消和超时消息同时竞争，数据库状态条件更新决定唯一获胜操作。

---

## 6. 状态机

### 6.1 排班状态

| 当前状态 | 操作 | 目标状态 |
|---|---|---|
| `DRAFT` | 发布 | `PUBLISHED` |
| `DRAFT` | 删除 | 物理删除或标记删除 |
| `PUBLISHED` | 正常关闭 | `CLOSED` |
| `PUBLISHED` | 异常取消 | `CANCELLED` |
| `CLOSED` | 无业务修改 | 终态 |
| `CANCELLED` | 无业务修改 | 终态 |

### 6.2 Slot 状态

- `OPEN`：时间未截止且排班已发布；是否满额由剩余容量判断。
- `CLOSED`：正常截止。
- `CANCELLED`：所属排班取消。

`FULL` 不作为可以任意写入的独立状态，`remaining_capacity = 0` 即代表满额，减少两份状态不一致。

### 6.3 预约状态

| 当前状态 | 操作 | 目标状态 | 是否归还号源 |
|---|---|---|---|
| `PENDING_CONFIRMATION` | 确认 | `BOOKED` | 否 |
| `PENDING_CONFIRMATION` | 主动取消 | `CANCELLED` | 是 |
| `PENDING_CONFIRMATION` | 超时关闭 | `EXPIRED` | 是 |
| `BOOKED` | 截止前取消 | `CANCELLED` | 是 |
| `BOOKED` | 等待线下就诊 | 保持 `BOOKED` | 否 |
| `CANCELLED` | 任意业务修改 | 拒绝 | 否 |
| `EXPIRED` | 任意业务修改 | 拒绝 | 否 |

V1 不继续扩展 `CHECKED_IN`、`IN_PROGRESS`、`COMPLETED` 等接诊状态。

---

## 7. 功能需求

### 7.1 认证与账号

- 使用 Spring Security 完成认证与授权。
- 密码使用 BCrypt。
- 登录成功返回 JWT Access Token。
- 账号支持 `ENABLED`、`DISABLED`、`LOCKED`。
- 退出或禁用后的令牌失效方案在 M1 设计阶段确定并测试。
- 未登录返回 401，无权限返回 403。
- 日志不得输出密码、Token 和完整手机号。

当前只支持一名用户一个固定角色，不创建通用权限配置平台。这样足够练习 RBAC，也避免把项目变成另一个 OA。

### 7.2 就诊人

- 患者账号可以创建多个虚构就诊人。
- 字段只保留姓名、性别、出生日期、模拟手机号和关系。
- 就诊人保存 `ownerUserId`，作为数据权限依据。
- 患者只能查询或修改自己名下记录。
- 对普通返回和日志中的手机号进行脱敏。

### 7.3 科室、医生与诊室

- 科室只做启停和查询，不实现复杂树。
- 医生绑定一个账号和一个科室。
- 诊室属于一个科室。
- 被停用的医生、诊室不能创建新排班。
- 只实现排班所需要的最小管理接口。

### 7.4 排班与 Slot

- 管理员创建未来排班，开始时间必须早于结束时间。
- 医生和诊室不能存在重叠的有效排班。
- 只有 `DRAFT` 排班可以发布。
- 发布和 Slot 生成必须在一个事务内完成。
- 患者可以按日期、科室、医生和是否有余号分页查询。
- 关闭或取消排班必须幂等。
- V1 排班取消只允许在没有有效预约时执行；批量迁移预约属于后续扩展。

### 7.5 预约

- 创建请求必须包含 `Idempotency-Key`。
- 只能为当前账号名下的就诊人预约。
- Slot 必须开放、未截止且有剩余容量。
- 同一就诊人在同一 Slot 只能存在一个有效预约。
- 创建成功进入 `PENDING_CONFIRMATION`。
- 只能在截止前确认或取消。
- 状态更新必须带当前状态条件。
- 每次有效状态迁移写入历史。
- 患者只能看自己的预约；医生只能看自己排班的预约；管理员按条件分页查询。

---

## 8. 数据模型范围

校招主线控制在 12 张业务表：

| 模块 | 表 | 关键用途 |
|---|---|---|
| 身份 | `sys_user` | 登录、角色、账号状态、令牌失效版本 |
| 患者 | `med_patient` | 就诊人和 `owner_user_id` |
| 组织 | `med_department` | 最小科室 |
| 组织 | `med_doctor` | 医生账号和科室绑定 |
| 组织 | `med_clinic_room` | 诊室 |
| 排班 | `med_schedule` | 医生、诊室、日期、时间和状态 |
| 号源 | `med_slot` | 时间段、总容量和剩余容量 |
| 预约 | `med_appointment` | 当前状态、确认/取消截止时间、有效标记 |
| 预约 | `med_appointment_status_log` | 状态历史 |
| 幂等 | `sys_idempotency_record` | 用户、操作、幂等键、处理结果 |
| 消息 | `sys_outbox_event` | 事务内待投递事件 |
| 消息 | `sys_consumed_message` | 消费者幂等记录 |

### 8.1 必须落到数据库的约束

- `sys_user.username` 唯一。
- `med_doctor.user_id` 唯一。
- `med_slot(schedule_id, start_time, end_time)` 唯一。
- `sys_idempotency_record(user_id, operation_type, idempotency_key)` 唯一。
- `sys_consumed_message(consumer_name, message_id)` 唯一。
- 有效预约唯一性必须由 MySQL 约束兜底，不能只做 Service 预查询。
- 状态历史、Outbox 和消费记录不做随意逻辑删除。

MySQL 不支持通用部分唯一索引。是否使用“有效标记 + NULL”的唯一约束方案，要在数据库设计课中明确其行为、优缺点和测试证据。

### 8.2 必须设计的联合索引

- 医生某日排班与时间冲突查询。
- 诊室某日排班与时间冲突查询。
- 患者按状态和时间查询预约。
- 医生按排班和时间查询预约。
- 患者可预约 Slot 条件查询。
- Outbox 按投递状态和下次重试时间扫描。

索引不能只写在建表语句中，必须用 10 万条测试数据保存优化前后的 `EXPLAIN` 和真实耗时。

---

## 9. 技术主线与取舍

### 9.1 MySQL 先保证正确性

首版预约不依赖 Redis：

```text
Spring 事务
+ 条件扣减 remaining_capacity > 0
+ 有效预约唯一约束
+ 状态 CAS
+ 幂等记录
```

必须比较并能解释：

- “先查余量再更新”为何存在竞态窗口。
- 条件更新与 `SELECT ... FOR UPDATE` 的适用区别。
- Java `synchronized` 为什么不能解决多实例数据库竞争。
- 业务预检查与数据库唯一约束为什么缺一不可。
- Spring 事务代理、自调用、异常类型和回滚规则。

### 9.2 Redis 只解决查询性能

主线中的 Redis 场景只有一个：热点 Slot 可用性查询缓存。

```text
查询：Redis -> 未命中查 MySQL -> 写入短 TTL 缓存
写入：MySQL 事务提交成功 -> 删除对应缓存
故障：Redis 不可用 -> 有界降级到 MySQL
```

要求：

- 使用 TTL 随机抖动降低同时过期风险。
- 不存在的 ID 使用短 TTL 空值或等价策略防穿透。
- 预约是否成功永远由 MySQL 决定。
- 能接受查询缓存短暂陈旧；创建预约仍会返回真实结果。
- 保存缓存命中、失效和 Redis 不可用的测试证据。

为什么不在主线中使用 Redis Lua 扣库存：

- 它会引入 Redis 与 MySQL 双写、补偿、对账和降级问题。
- 在 MySQL 正确性尚未完全验证前，引入它只会掩盖核心问题。
- 完成 P1 后若选择此 P2 扩展，必须把失败补偿和对账一起实现。

### 9.3 RabbitMQ 只解决超时关闭

唯一场景：`PENDING_CONFIRMATION` 预约超过确认时间后自动关闭。

可靠性链路：

1. 预约和 Outbox 事件在同一 MySQL 事务内提交。
2. Relay 扫描未投递事件并发布消息。
3. Publisher Confirm 成功后更新投递状态。
4. RabbitMQ 使用 TTL + Dead Letter Exchange 或等价延迟方案。
5. 消费者先通过唯一消息记录保证幂等。
6. 业务事务内 CAS 关闭预约并归还号源。
7. 提交成功后 Ack；失败进入有限重试和死信。
8. Relay 或消费者崩溃后，系统重启能够继续处理。

Outbox 不是“多建一张表”就完成，必须具有扫描投递、失败重试、状态记录和恢复测试。

### 9.4 可观测性只做排障所需

- 请求 Trace ID。
- 结构化日志。
- Actuator 健康检查。
- 预约成功、号源不足、幂等命中、Outbox 待投递、消息重试等少量业务指标。
- 不在主线搭建完整 Prometheus/Grafana 大屏。

---

## 10. 核心接口范围

接口统一使用 `/api/v1`，控制在约 20 个核心接口：

| 模块 | 接口 |
|---|---|
| 认证 | `POST /auth/login`、`POST /auth/logout` |
| 就诊人 | `POST /patients`、`GET /patients`、`GET /patients/{id}` |
| 基础查询 | `GET /departments`、`GET /doctors`、`GET /clinic-rooms` |
| 排班 | `POST /admin/schedules`、`POST /admin/schedules/{id}/publish`、`POST /admin/schedules/{id}/close` |
| 号源 | `GET /slots`、`GET /slots/{id}` |
| 预约 | `POST /appointments`、`POST /appointments/{id}/confirm`、`POST /appointments/{id}/cancel`、`GET /appointments/{id}`、`GET /appointments` |
| 医生视角 | `GET /doctor/schedules`、`GET /doctor/appointments` |

接口规范：

- DTO 接收输入，VO 返回输出，不暴露 Entity。
- 参数校验失败返回 400。
- 未登录返回 401，无权限返回 403。
- 资源不存在返回 404。
- 状态冲突、重复预约或号源不足返回 409。
- 创建预约使用 `Idempotency-Key` 请求头。
- 分页限制最大页大小。
- OpenAPI 中给出成功和主要失败示例。

---

## 11. 自动化测试与硬性证据

测试不能集中到最后一周。每完成一个业务规则，当周同步补测试。

| 编号 | 必须验证的场景 | 通过标准 |
|---|---|---|
| E-01 | 最后号源竞争 | 容量 50、500 个并发请求，恰好 50 个有效成功，剩余 0 |
| E-02 | 同一幂等键并发 | 多次请求只创建一条预约、只扣减一次，并返回同一业务结果 |
| E-03 | 不同键重复预约 | Service 预检查和数据库唯一约束共同阻止重复有效预约 |
| E-04 | 跨表事务回滚 | 人为制造预约插入失败，Slot 扣减一起回滚 |
| E-05 | 并发重复取消 | 只有一次状态迁移成功，容量只增加 1 |
| E-06 | 取消与超时竞争 | 最终只有一个终态，容量只归还 1 |
| E-07 | 重复超时消息 | 同一消息重复消费不会重复关闭或归还 |
| E-08 | MQ 故障恢复 | Broker 或消费者中断后，Outbox 能继续投递并最终完成 |
| E-09 | 非法状态迁移 | 所有非法迁移返回明确冲突且数据库不变 |
| E-10 | 患者越权 | 患者 A 不能访问患者 B 的就诊人和预约 |
| E-11 | 医生越权 | 医生 A 不能读取医生 B 排班下的预约 |
| E-12 | 排班并发冲突 | 并发创建重叠排班时只能有一个有效结果 |
| E-13 | Redis 降级 | Redis 不可用时查询可降级，预约仍不超卖 |
| E-14 | SQL 优化 | 10 万条数据保存索引前后执行计划与耗时 |
| E-15 | 空环境启动 | 空库通过 Flyway，Docker Compose 可启动依赖与应用 |

证据包必须包含：

- JUnit 单元测试。
- Spring 集成测试和 MockMvc 权限测试。
- Testcontainers 的真实 MySQL、Redis、RabbitMQ 测试。
- Java 并发测试或 HTTP 压测脚本。
- `EXPLAIN` 报告。
- MQ 重复、宕机和恢复记录。
- Docker 启动与烟雾测试命令。

“本机 500 并发”只能称为并发一致性验证，不能包装成生产级高并发或虚构 QPS。

---

## 12. 面试能力映射

| 面试主题 | 项目中的真实证据 |
|---|---|
| IOC 与分层 | 模块边界、依赖注入、Controller/Service/Mapper 职责 |
| Spring MVC | 参数绑定、Validation、异常转换、HTTP 状态码 |
| Spring Security | FilterChain、SecurityContext、JWT、RBAC、数据权限 |
| Spring 事务 | 预约扣减与创建、取消归还、Outbox 同事务 |
| AOP 代理 | `@Transactional` 生效与失效场景 |
| MySQL 索引 | 预约查询、排班冲突、Outbox 扫描和 `EXPLAIN` |
| MVCC 与锁 | 条件更新、资源行锁、当前读、并发冲突 |
| Java 并发 | 线程池发起竞争、竞态条件、结果汇总 |
| 幂等 | Idempotency-Key、唯一约束、消费记录 |
| 状态机 | 确认、取消、超时的合法迁移与 CAS |
| Redis | Cache Aside、TTL、穿透、失效和降级 |
| RabbitMQ | Confirm、Ack、重试、DLX、重复消费、Outbox |
| 工程能力 | Flyway、Testcontainers、Docker、CI、日志和健康检查 |
| 系统设计 | 模块化单体、数据库事实源、同步与异步取舍 |

### 12.1 必须准备好的五个项目故事

1. **并发预约**：初始“先查再减”的问题、方案比较、最终条件更新、失败回滚和测试结果。
2. **幂等与状态竞争**：重复点击、唯一约束、取消与超时竞争以及只归还一次。
3. **可靠超时关闭**：数据库与 MQ 双写问题、Outbox、重复消费和故障恢复。
4. **数据权限**：角色权限与对象归属权限的区别，以及越权测试。
5. **性能与证据**：索引选择、`EXPLAIN`、Redis 缓存边界和真实测试数据。

每个故事使用同一结构：

```text
业务问题
  -> 最初方案
  -> 失败窗口
  -> 方案比较
  -> 最终实现
  -> 故障处理
  -> 测试证据
  -> 当前限制
```

---

## 13. 八周交付路线

| 周次 | 项目主线 | 核心知识 | 周末可验证交付物 |
|---|---|---|---|
| W1 | 范围冻结、设计、骨架、Flyway、Web 规范 | Java 对象职责、Maven、IOC、MVC、异常、HTTP | 应用启动，空库迁移，统一异常和首批测试通过 |
| W2 | Spring Security、JWT、账号状态、患者所有权 | FilterChain、BCrypt、401/403、RBAC、数据权限 | 登录和越权测试通过 |
| W3 | 科室/医生/诊室、排班、冲突、发布与 Slot | Java 时间、区间冲突、行锁、索引、状态机 | 并发排班冲突和发布原子性测试通过 |
| W4 | 预约创建、确认、取消、幂等、状态历史 | 事务代理、条件更新、唯一约束、MVCC、CAS | MySQL 版预约规则和回滚测试通过 |
| W5 | 并发一致性与 SQL 优化 | 线程池、竞态、锁与死锁、B+ 树、`EXPLAIN` | 500 抢 50 和 10 万数据优化报告 |
| W6 | Redis 热点号源查询缓存 | Cache Aside、TTL、穿透、雪崩、失效和降级 | 缓存命中、失效、故障降级测试 |
| W7 | RabbitMQ 超时关闭与 Outbox | Exchange、Confirm、Ack、重试、DLX、消费幂等 | 重复消息和故障恢复测试通过 |
| W8 | Docker、Actuator、CI、证据包和面试口述 | Linux、容器网络、日志、JVM 排障基础 | 一键启动、固定演示、README、简历与题库 |

算法和通用八股每天保持 30—40 分钟独立学习，不用继续给项目增加业务模块来“承载”所有知识点。

---

## 14. 简历表述模板

以下内容只有对应证据通过后才能使用，花括号中的数字必须替换为真实结果。

### 14.1 项目介绍

> MedFlow 是基于 Java、Spring Boot 构建的门诊资源调度与可靠预约系统，围绕排班发布、有限号源分配和预约生命周期，重点解决并发超卖、重复请求、状态竞争、数据权限及异步超时任务可靠性问题。

### 14.2 候选亮点

- 使用 MySQL 事务、条件更新和唯一约束保证预约一致性，在容量为 {N}、并发请求 {M} 的测试中，成功数严格等于容量且库存不为负。
- 通过 Idempotency-Key、状态 CAS 和状态历史，保证重复预约、重复取消及取消与超时竞争只产生一次有效业务结果。
- 使用 RabbitMQ 与 Outbox 实现待确认预约超时关闭，通过消费幂等、重试和死信处理消息重复与故障恢复。
- 使用 Spring Security 实现三角色 RBAC 和对象级数据权限，自动化测试证明患者及医生无法越权读取他人预约。
- 使用 Redis Cache Aside 加速热点号源查询，保留 MySQL 最终校验，并验证缓存失效和 Redis 故障降级。
- 使用 Testcontainers、并发测试和 `EXPLAIN` 验证事务正确性、可靠消息及索引优化，并通过 Docker Compose 提供可复现环境。

最终简历只保留证据最强的 3—4 条，不复制整份技术清单。

---

## 15. 完成标准

### 15.1 功能完成

- 三角色的核心接口可以演示。
- 从排班发布到预约确认/取消/超时形成闭环。
- 五条核心不变量都有自动化测试。
- 所有重要状态变化有历史记录。

### 15.2 工程完成

- Maven Wrapper、Java 25 和配置方式明确。
- 空库可以通过 Flyway 初始化。
- 测试不依赖开发者手工预置某条数据库记录。
- Docker Compose 能启动所需环境。
- 日志和健康检查能够定位失败阶段。

### 15.3 面试完成

- 五个项目故事可以脱稿讲清。
- 所有简历数字有脚本和结果文件。
- 能比较至少两种并发控制方案。
- 能说明 Redis、MQ 的失败场景和当前限制。
- README 中其他人可以复现启动、测试和演示。

只有同时满足“实现、编译、自动化测试、手工验收、口述、Git 证据”才算真正完成。

---

## 16. 范围冻结规则

1. MySQL 版没有证明不超卖，不引入 Redis。
2. 幂等和取消只归还一次没有测试，不引入 RabbitMQ。
3. Outbox 没有 Relay、重试和恢复测试，不能写“可靠消息”。
4. Redis 只做查询缓存，不在主线增加 Lua 库存双写。
5. P0/P1 未全部验收，不增加排队、随访、AI、FHIR 或微服务。
6. 每个亮点必须包含“问题、比较、失败处理、验证结果”。
7. 没有运行证据的内容不能写入简历。
8. 做不完时优先砍 P2，而不是削弱测试、文档和故障验证。

---

## 17. 当前下一步

M0 需求与设计已于 2026-09-02 通过验收，当前处于 M1 项目骨架与统一规范。

Day 5 工程初始化已完成：

1. 已初始化 `main` 分支 Git 仓库。
2. 项目编译和运行工具链已统一为 Java 25。
3. 已建立 Maven `pom.xml` 和 Spring Boot 3.5.16 最小项目骨架。
4. Maven Wrapper 已固定为 Maven 3.9.16。
5. 上下文测试、可执行 JAR 打包和 HTTP 启动验收已通过。
6. 未提前引入数据库、MyBatis-Plus、Redis、RabbitMQ 或业务模块。

当前唯一下一步：完成 Day 5 的 Maven 生命周期、IOC 与自动配置口述验收；随后进入 Day 6 Flyway 与统一 Web 规范。
