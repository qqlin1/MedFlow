# 从业务规则设计表与索引（MedFlow 练习表）

> **可填写的交互版在 `DAY3_SCHEMA_WORKSHEET.html`**（同目录，浏览器打开即可，
> 填写内容自动存在本机，还能一键导出答案）。本 md 是纯文本版，方便在 IDE 里看和搜索。
> （本文件属于 Day 3「ER 模型与数据库约束」的补充练习；Day 4 正课见
> `DAY4_API_PERMISSION_TIMING.html`。）

## 核心命题

> **每一条唯一索引，都是一句业务规则的翻译。**
> **每一条普通索引，都是一次查询路径的固化。**
>
> 你不是在建索引，你是在**把业务规则钉进数据库**。

索引不是技巧问题，是"你有没有把业务规则想清楚"的问题。
字段不是自己设计的 → 就永远只能背索引语法，说不出它为什么存在。

---

## 第一部分：推导框架

### A. 推唯一索引 —— 从规则推，问三个问题

拿到一句中文业务规则，问三遍：

| # | 问题 | 答案长什么样 |
|---|---|---|
| ① | **约束谁？** | 主体字段（patient_id / user_id / doctor_id） |
| ② | **在多大的范围里？** | 边界字段（slot_id / schedule_id / work_date） |
| ③ | **什么情况下这条规则不算数？** | 状态 / 有效性字段（active_flag / status） |

**第 ③ 问最容易漏，也最值钱。** 因为业务规则几乎都有时效性：
"不能重复预约"通常是"不能重复**有效**预约"，取消过的、超时的是允许重复的。
漏了第 ③ 问 → 用户取消一次就永远约不了了。

三个问题的答案，就是联合唯一索引的那几个字段。

### B. 推普通索引 —— 从查询推，方向相反

| # | 问题 |
|---|---|
| ① | 这张表上会发生哪些查询？（翻接口清单，一个一个列） |
| ② | 每个查询的 `WHERE` / `ORDER BY` 里有哪些字段？ |
| ③ | 这些字段按什么顺序排？ |

**最左前缀不用背，它是第 ③ 问的副产品**：
联合索引 `(a, b)` 能加速 `a`、能加速 `a + b`，**不能加速单独的 `b`**。
所以字段顺序由查询决定，不是按字母排、也不是按重要性排。

### C. 校验

> **每个索引都必须能回答"它在防什么、或在加速什么"。**
> 答不上来的，删掉。多余索引会拖慢写入，还会让 UPDATE 多锁不必要的行。

---

## 第二部分：完整示范 —— `med_appointment`

### 第一步：把业务规则写成中文（从需求文档抄）

- E-03：不同键重复预约 → Service 预检查和数据库唯一约束共同阻止**重复有效预约**
- 状态机：`PENDING_CONFIRMATION` / `BOOKED` / `CANCELLED` / `EXPIRED`
- 取消与超时都要归还号源（说明取消、超时后该号源可以被重新预约）

提炼成一句：**同一个就诊人，在同一个号源上，最多只能有一条处于有效状态的预约。**

### 第二步：问三个问题

| # | 问题 | 答案 | 字段 |
|---|---|---|---|
| ① | 约束谁？ | **就诊人**（不是登录账号 —— 可以给家人挂号） | `patient_id` |
| ② | 在多大的范围里？ | 这个号源。`slot_id` 本身已含"某医生+某天+某时段" | `slot_id` |
| ③ | 什么情况不算数？ | 已取消 `CANCELLED`、已超时 `EXPIRED` | `active_flag` |

### 第三步：落成索引

```sql
ALTER TABLE med_appointment
  ADD COLUMN active_flag TINYINT
      AS (CASE WHEN status IN ('PENDING_CONFIRMATION','BOOKED') THEN 1 ELSE NULL END) VIRTUAL,
  ADD UNIQUE KEY uk_patient_slot_active (patient_id, slot_id, active_flag);
```

**为什么用生成列 + NULL**：MySQL 没有部分唯一索引（不能写 `WHERE status IN (...)` 的唯一索引），
但它允许唯一索引里出现多个 NULL。已取消 / 已超时的行 `active_flag = NULL` → 不参与唯一性判断。

### 第四步：推普通索引（从接口清单）

| 查询从哪来 | SQL | 索引 |
|---|---|---|
| `GET /appointments` 我的预约列表 | `WHERE patient_id = ? ORDER BY created_at DESC` | `(patient_id, created_at)` |
| 超时关闭定时任务 | `WHERE status='PENDING_CONFIRMATION' AND confirm_deadline < NOW()` | `(status, confirm_deadline)` |
| `GET /doctor/appointments` | `WHERE doctor_id = ? AND work_date = ?` | 需**冗余**这两个字段才能建 |

**冗余字段的判断标准**：如果一条查询会被高频执行，而它需要的字段在另一张表上，
就在本表冗余一份（插入时带过来，**不可变**），为的是能建索引。
这个取舍本身就是面试故事：**愿意为保证业务规则/查询性能付出存储代价，而不是把校验甩给应用层。**

---

## 第三部分：练习表（自己填，做完发我）

下面每张表，按三步填：
**① 写出业务规则（中文一句）② 问三问，得出唯一索引 ③ 列查询，得出普通索引**

### 1. `sys_user`（登录身份）

- 业务规则：
- 三问：① 约束谁？ ② 范围？ ③ 什么情况不算数？
- 唯一索引：
- 普通索引（从哪些查询来）：

> 提示：想一想"用户名/手机号能不能重复""删掉的账号能不能重新注册同一个手机号"。
> 第 ③ 问在这里就是一个真决策。

### 2. `med_patient`（就诊人）

- 业务规则：
- 三问：① 约束谁？ ② 范围？ ③ 什么情况不算数？
- 唯一索引：
- 普通索引：

> 提示：就诊人挂在 `owner_user_id` 下。同一个登录账号能不能建两个同名就诊人？
> 就诊人的身份证号要不要唯一？唯一的**范围**是"全表"还是"同一个 owner 下"？

### 3. `med_schedule`（排班）

- 业务规则：
- 三问：① 约束谁？ ② 范围？ ③ 什么情况不算数？
- 唯一索引：
- 普通索引：

> 提示：一个医生同一天同一时段，能不能有两条**有效**排班？
> 已被取消的排班（状态 `CANCELLED`）要不要占这个约束？
> 注意：时间重叠判断（8:00-10:00 和 9:00-11:00）**唯一索引管不了**，
> 只能靠 `SELECT ... FOR UPDATE` 锁住范围再判断 —— 这是路线一第五节讲过的。
> **想清楚"哪些约束索引能管、哪些管不了"，这本身就是加分项。**

### 4. `med_slot`（号源）

- 业务规则：
- 三问：① 约束谁？ ② 范围？ ③ 什么情况不算数？
- 唯一索引：
- 普通索引：

> 提示：Slot 由 Schedule 生成。一条 Schedule 会生成多个时间段（Slot），
> 所以约束是 `(schedule_id, start_time, end_time)` 还是别的？
> 另外：扣减号源要用条件更新，那 `WHERE id = ?` 命中主键就够了，**不需要额外索引**。
> 但"患者浏览可预约号源"的查询 `WHERE work_date = ? AND status='OPEN'` 呢？

### 5. `sys_idempotency_record`（幂等记录）

- 业务规则：
- 三问：① 约束谁？ ② 范围？ ③ 什么情况不算数？
- 唯一索引：
- 普通索引：

> 提示：第 ③ 问在这里的答案是"**失败不算数**"——
> 业务失败 → 整个事务回滚 → 幂等记录自动消失 → 同 key 可真正重跑。
> 所以这张表的唯一索引里**没有状态字段**，这跟 `med_appointment` 正好相反。
> **想清楚为什么一个要状态字段、一个不要。**

### 6. `sys_outbox_event`（待投递事件）

- 业务规则：
- 三问：① 约束谁？ ② 范围？ ③ 什么情况不算数？
- 唯一索引：
- 普通索引：

> 提示：Relay 扫描要跑 `WHERE status='PENDING' ORDER BY created_at LIMIT 100`，
> 而且多实例并发扫描要用 `FOR UPDATE SKIP LOCKED`。索引要服务这条查询。

### 7. `sys_consumed_message`（消费者幂等）

- 业务规则：
- 唯一索引：

> 提示：这张表最简单。Outbox 保证"不丢"，它保证"不重"。
> 想想它的唯一键需要几个字段、需不需要状态字段。

### 8. `med_appointment_status_log`（状态历史）

- 业务规则：
- 唯一索引：
- 普通索引：

> 提示：这张表**大概率不需要唯一索引**。为什么？
> 因为它记录的是"发生过的事实"，事实可以重复（同状态可以记录多次？还是不行？）。
> 想清楚"要不要防重复记录"这个决策本身。

---

## 第四部分：自检

填完以后，逐条检查：

1. 每条**唯一索引**，你都能说出它对应的那句中文业务规则 —— 说不出就是多余的
2. 每条唯一索引，你都认真回答过第 ③ 问（什么情况不算数）—— 漏了就会出"取消后永远约不了"这类 bug
3. 每条**普通索引**，你都能说出它服务哪条查询 —— 说不出就是多余的
4. 每个联合索引的**字段顺序**，你都能说出为什么是这个顺序（最左前缀）
5. 你能指出**哪些业务规则索引管不了**（比如时间重叠判断），并说出替代方案

第 5 条最难，也最值钱。**能说出"这个约束我管不了，所以用了别的办法"，
比能背出十个索引语法强得多。**
